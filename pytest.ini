[pytest]
asyncio_mode = auto
testpaths = tests
addopts = --strict-markers
markers =
    dbschema: tests que requieren un Postgres efímero con pgvector (CI job db-schema)
    live: tests de integración live contra APIs reales (ElevenLabs, OpenAI, Supabase, Redis)
